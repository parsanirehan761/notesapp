# notesapp
My Learning Notes
An offline-first personal notebook built with HTML, CSS, and vanilla JavaScript. Notes, subjects, tags, and preferences are stored only in the browser's IndexedDB.

Open index.html directly for local use, or serve this folder from any static web server to enable the service worker and PWA installation. There are no accounts, analytics, external APIs, or dependencies.

